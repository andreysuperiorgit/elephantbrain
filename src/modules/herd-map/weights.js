/** How much each kind of evidence counts toward an edge. */

export const W_SAME_BLOCK = 1.0;     // per token both hit within the window
export const W_FUNDER = 2.0;         // shared funding source

export const CAVEAT = "Coordinated behaviour is not proof of shared ownership.";
